# Read: Why we need dotenv if we have process.env?

# Read: About brower CORS and need of cors?

# Read: Why we need express if we have http in node?

# Learn: Docker

# Read: How to define user schemas.

# Pactice: Desturcturing

# TIP: Generate jwt key node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
